// This component is no longer used - DJ cards are now embedded directly in DJDirectory.tsx
// to match the flight booking interface design exactly.

import React from 'react';

const DJCard: React.FC = () => {
  return (
    <div>
      This component has been replaced by inline cards in DJDirectory.tsx
    </div>
  );
};

export default DJCard;
